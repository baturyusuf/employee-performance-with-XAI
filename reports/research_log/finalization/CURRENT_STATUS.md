# Current Status

- Current phase: interruption recovery and V2-021a implementation/testing are complete; README/log synchronization and checkpoint review are in progress. The approved seven-figure plan is frozen without generating figures or changing release readiness. The HRDataset_v14 production stage remains independently validated and untouched; the complete v2 core/supplementary package is still not built.
- Branch/remote: tested V2-021a checkpoint is `6da8273b458fd249d47d9bb5c75ebe9ff364617f` on `finalization/leakage-aware-v2`, six commits ahead of remote `e25f403d82082f97e34aa4f8174bfc001fced5d8` before this documentation synchronization. The earlier normal non-force push failed because noninteractive HTTPS credentials were unavailable; no credential setting was changed.
- Interruption recovery on 2026-07-14 re-read the required handoff/log/Unit 2G records, inspected branch/HEAD/status/diffs/15 commits, enumerated Python/Git/Node/test/model/shell processes and all TCP/UDP endpoints, and inspected the stage root for lock/partial/temp/staging residue. No Python/pytest/model process, repository/run listener, duplicate experiment or residue existed. Numerous generic IDE Git metadata queries did not identify this repository in their command lines and had ambiguous ownership, so they were conservatively left untouched. No process was terminated and no research file was deleted.
- Validated stage: local run `stage_validation_hrdataset_20260713T175045Z_5af0262e83a3`, config hash `5af0262e83a3720f8dca0b4d6795bdffc6bb2cefedc901ae0a47f9262d07f305`, scientific-input hash `71f1fc4699113876d70c6853abfdc8d3ec4f9419bb88897414fae28ae166422a`, source-tree hash `706690fcc28c8e308dcb667872035f72c8382bcc02f8585698a0968a8b8f873a`.
- Atomic integrity: `external_replication/stage_contract.json` is `complete`, `closed_world` and stage-relative. All 124 contract outputs and all 122 artifact-manifest rows match the local filesystem path, size and SHA-256; all are non-empty. The stage contains 125 files plus its provisional input manifest.
- Scientific integrity: 311 samples are assigned exactly once to ten outer folds; every outer-training partition has exact five-fold inner coverage and no outer-test leakage. All 400 candidate fits, 50 persisted outer models, 1,555 raw OOF rows, 50 calibration fits, 2,799 calibration-training OOF rows, ten calibrator/model relationships, 311 sigmoid OOF rows, and 5,000 paired bootstrap draws replay and bind to their declared fold/model/input identities.
- Explainability integrity: 6,531 local grouped SHAP rows equal `311 x 3 classes x 7 features`; all ten exact OOF model hashes match prediction receipts, prediction replay error is zero, maximum additivity error is `4.47e-06`, forbidden features are absent, and no explanation model was refit. The 45 fold-pair stability rows remain descriptive dependent comparisons with no confidence interval.
- External-stage results are stage-validation evidence only. Raw conservative macro-F1 is `0.666355` (95% paired-bootstrap CI `0.628073` to `0.704690`) and QWK is `0.541220` (`0.485143` to `0.598791`). Predeclared sigmoid improves log loss from `0.536253` to `0.422393` and Brier from `0.301308` to `0.235226`, but reduces macro-F1 by `-0.041104` (`-0.074614` to `-0.008355`) and predicts no class-4 argmax cases. These values are not frozen manuscript claims.
- The temporality-restricted audit lowers macro-F1 by `-0.366008` (`-0.412759` to `-0.319906`) relative to the conservative primary, demonstrating major dependence on timing-unverified engagement/attendance variables. Department-including, job-role-free and proxy-rich macro-F1 differences are inconclusive. SHAP ranks `DaysLateLast30`, `EngagementSurvey`, tenure and `Absences` highest; this is attribution, not causality.
- Support/claim boundaries: subgroup outputs enforce support 30 and metric denominator 10; 30 of 85 disparity rows are headline-eligible descriptive estimates. Department reconstructability is correctly `not_estimated_insufficient_outer_training_class_support` with zero models fitted. Locked INX transport is infeasible because only three safe common features meet a five-feature gate. Licence/source authenticity and mapped-target semantic equivalence remain unverified.
- Noncanonical status: the outer `stage_validation_input_manifest.json` intentionally remains `status=running`, has no end timestamp/command record, and there is no package `run_manifest`, final evidence manifest, package status, claim matrix or `latest` promotion. Current-HEAD run-manifest validation correctly rejects the provisional identity because `HEAD` changed after generation. This does not invalidate the atomic stage and is not a reason to rerun Unit 2G.
- D5 correction: commit `e25f403` accidentally pushed the complete 65,412,766-byte noncanonical stage package (including 50 models and row-level derivatives). The validated 126 files remain intact locally, but a normal forward checkpoint removes them from the current Git tip and an exact `.gitignore` rule prevents recommit. No history rewrite or force-push is permitted; the already-pushed blobs remain a documented repository-history/publication blocker.
- V2-029 implementation: subgroup diagnostics fail closed unless their source is exact raw OOF evidence with valid fold-model/probability receipts; both subgroup CSVs and metadata bind a deterministic semantic digest of the consumed policy-scoped rows plus its algorithm/column contract. External SHAP config/provider contracts pin `xgboost_raw_margin_score` attribution units and `xgboost_raw_margin` additivity space across numeric tables, fold receipts, metadata and local JSON/Markdown reason codes. The canonical config hash is now `ac32f7d80695e95adbad458ef31d9f1790b16e1eec306aaba57c5233f304e2f8`; scope hashes remain `af80b8a7...`/`18bbb5cb...`.
- V2-021a implementation: canonical config now defines exactly seven source-bound leakage-aware core figures, portable PNG/SVG/source-data/caption identities, four provenance identity fields and figure-specific claim boundaries. `src/governance/core_figure_contract.py` rejects ordering, prohibited scope, nonportable/wrong-stage/duplicate sources, plan drift and premature release readiness. No runner or artifact was added. Canonical config hash is now `eef3539be6470644cc1b3892e1aa6bb8c3186aeb9df0d61ec56635a64e978a44`; scope hashes remain unchanged.
- Latest gates: focused figure/core tests passed 36 with one historical skip; expanded config/scope/manifest/figure tests passed 114 with two skips; fixture integration focus passed 58. Independent review passed 25 with one skip and found no P0 in V2-021a. Full pytest passes 671 with 3 skips and 11 subtests in 111.10 seconds; unittest passes 178 with 2 skips in 8.012 seconds. Compileall and `pip check` pass.
- Worktree was clean immediately after checkpoint `6da8273`; only commit-hash documentation synchronization is now in progress. The local stage package remains preserved/ignored, `.idea/misc.xml` remains locally excluded, and the manuscript and validated stage artifacts are unchanged.
- Known blockers: the tested local commits are not on the remote because Git HTTPS/Git Credential Manager did not complete; no complete current-commit core/supplementary rebuild exists; both scopes remain `release_ready:false`. The SHAP stage still emits obsolete publication-numbered Figure 6/7 previews and the legacy seven-figure validator encodes v1 stems; both are P1 blockers before a v2 figure generator/canonical build. Tables, dependency lock, CI/global no-network gate, supplementary work and sanitized data transition remain open. Ethics and dataset source/licence/citation verification remain external manual blockers. V2-029 is implemented but its fields are not scientific evidence until a future canonical rebuild emits and validates them.
- Exact next action: commit this hash synchronization, attempt one authorized normal non-force push, record its result, then begin V2-032 without rerunning Unit 2G.
- Decisions awaiting the user: none for this checkpoint. Ethics/source/licence facts and later claim-matrix approval remain external/manual gates and will not be invented.
- Paid API calls: zero. Dataset/network acquisition calls: zero. Manuscript edits: zero.

Historical model-selection decision evidence remains unchanged: no baseline met the positive macro-F1 difference plus positive paired-bootstrap CI-lower gate, so XGBoost remains the predeclared XAI reference.
