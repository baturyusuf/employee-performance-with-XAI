# Persistent Decision Log

## 2026-09-03 broader major-revision extension

- The user supplied an explicit 42-part reviewer-remediation brief and requested periodic GitHub pushes.
- The v2 freeze remains binding for v2 bytes, identities, and claims; it does not imply that the newly supplied broader brief is complete.
- Missing work will be additive under a new v3 evidence identity. Existing `canonical_v2_20260714T221501Z_483f96f` artifacts must not be overwritten, relabelled, or silently mixed with v3 results.
- The brief explicitly authorizes the requested ordinal, repeated-CV, independent-retuning, SHAP-faithfulness, replication-sensitivity, naive-baseline, per-class, and data-quality investigations. Exact v3 protocol choices must be prespecified in the v3 plan/config before real-data execution.
- Manuscript Markdown/LaTeX remains protected by the existing claim-approval gate. Planning, contract implementation, experiments, compact reports, and claim-matrix preparation may proceed; manuscript authoring waits for the final v3 claim matrix and user approval.
- Tested, reviewable checkpoints should be committed and pushed periodically. Force-push, history rewriting, merge, release publication, DOI publication, raw-data publication, and paid API calls remain prohibited without separate authorization.

## 2026-07-16 aggregate publication-assets decision

- The user explicitly authorized GitHub publication of the final tables and figures so remote authoring/review agents can access the canonical numerical cells and data-derived graphics.
- Track the complete aggregate manuscript-support subset unchanged: Figures 1-7 as PNG/SVG, seven source CSVs, seven captions, eleven core tables, three supplementary tables, stage/asset manifests, and both claim-boundary files.
- Keep raw/interim data, employee-level OOF/local-SHAP records, fitted models/calibrators, bootstrap arrays, candidate/training-partition records, caches, and temporary outputs ignored. These are not required to resolve the remote manuscript-evidence gap and remain subject to licence, privacy, history, and Release/Zenodo decisions.
- This is a publication-access change only. It does not rerun science, change canonical bytes, alter the frozen protocol, authorize manuscript claims, publish a release, or resolve ethics/licence blockers.

## 2026-07-14 canonical admission decision

- Admit `canonical_v2_20260714T221501Z_483f96f` as the sole canonical v2 technical evidence package.
- The package was generated from clean pushed commit `483f96f`, passed strict post-promotion validation, and is referenced only through pointer-only `latest` plus compact receipt `../finalization_v2/15_canonical_evidence_receipt.json`.
- Reject every earlier stage-validation, failed, pre-migration, pre-portability, or historical v1 candidate as canonical evidence.
- Freeze scientific code/config and stop further experiments unless a concrete integrity failure is proven.
- Do not begin manuscript writing in this task. Ethics, provenance/licence, historical-publication strategy, claim approval, manuscript authoring, and release publication remain manual units.

The fixed decisions are recorded in `../finalization_v2/00_scope_and_fixed_decisions.md` and `../finalization_v2/04_decision_log.md`.

Accepted on 2026-07-13:

- D1: Logistic Regression + Random Forest + LightGBM.
- D2: restrained nested tuning with 10 outer × 5 inner folds and outer-test isolation.
- D3: user-provided pinned files for all datasets; sanitized publication export; only approved-manifest URL acquisition when local data is absent.
- D4: application pending; institution/unit/reference/date remain unknown because supplied strings were placeholders.
- D5: small artifacts in Git; full package prepared for Release/Zenodo; pointer-only latest; no publication yet.
- Dedicated branch/commit permission: `finalization/leakage-aware-v2`, tested checkpoints only.
- Model selection: macro-F1 is the sole primary inner-CV metric; QWK is secondary and is used only inside the inclusive absolute `0.001` macro-F1 tie pool, followed by lowest candidate index.
- Stop gate: trigger only when baseline-minus-XGBoost macro-F1 is positive and the paired OOF bootstrap 95% CI lower bound is greater than zero; then request user direction before choosing the XAI reference model.
- Bootstrap: 5,000 paired sample-level OOF draws, stratified by outer fold and true class, percentile 95% interval with linear quantiles.

Observed on 2026-07-13: verified trial `benchmark-10x5-20260713-6a80074` did not trigger the baseline superiority gate. XGBoost therefore remains the predeclared XAI reference without a new user decision. Secondary QWK differences remain reportable but do not override the accepted macro-F1 gate.

Engineering decision on 2026-07-13: canonical SHAP must load the exact prediction-producing XGBoost pipeline for each outer fold, replay its OOF evidence at `1e-12` probability tolerance, and contain no model/preprocessor fit or splitter path. The 45 fold pairs are dependent descriptive units, so no pairwise t confidence interval is reported. The historical `6a80074` trial remains immutable and reader-verifiable at its documented `1e-6` tolerance, but its pre-warning-cleanup config/probabilities and missing nested one-hot feature-name lineage make it intentionally ineligible as a canonical SHAP upstream. No user scientific decision was required.

Engineering/scientific consequence of accepted D2 on 2026-07-13: leakage-policy variants are a matched feature-access sensitivity analysis, not independently optimized leaderboard entries. The exact benchmark OOF rows are reused for the primary policy; every other declared policy receives the primary policy's selected XGBoost candidate from the same outer fold. All policy intervals/differences use the same 5,000 paired sample-level resamples as the benchmark. The legacy full-feature name is retained, but its role is an information-rich diagnostic comparator whose superiority is not guaranteed. No new user decision was required because D2 already says not to tune each leakage policy independently.

Accepted on 2026-07-13: calibration option A. For each outer fold, five inner-fold XGBoost refits must generate cross-fitted probabilities for every outer-training sample exactly once. The sigmoid calibrator may see only those inner-OOF probabilities and labels. It is applied only to the exact persisted benchmark fold model's untouched outer-test probabilities. Outer-test data cannot influence tuning, calibrator fitting, method selection or threshold selection. Raw is a comparator, sigmoid is the fixed primary method, and isotonic is excluded from the core protocol.

Unit 2E engineering decisions on 2026-07-13: use the warning-free scikit-learn `>=1.8,<1.9` L2 API with one numerical thread; preserve upstream float probabilities exactly and normalize only the calibrated projection; freeze 95% paired percentile bootstrap strata to outer fold plus true class; and revalidate every config/data/fold/benchmark/model input immediately before atomic publication. The current config hash must reject the historical benchmark hash. The expensive rerun is deferred until all remaining core config and hashed inputs freeze, avoiding another knowingly stale result package. These choices do not alter the accepted model, folds, primary metric or sigmoid decision.

Unit 2F engineering consequence on 2026-07-13: subgroup diagnostics will consume the exact raw OOF probabilities and labels from the three declared `policy_ablation` systems; they will not fit a performance model or mix calibrated primary probabilities with raw comparison-policy probabilities. Their 5,000-draw plan must reproduce the accepted `(outer_fold, y_true)` policy resample hash. Department reconstructability will use the exact shared outer-fold assignment but a separate 5,000-draw plan stratified by `(outer_fold, EmpDepartment)` because it is a different target. The proxy target is removed before feature-contract hashing; therefore the department-including policy is an explicit alias of the department-free predictor contract and is not refitted. These are required consequences of the accepted shared-fold, support-aware and proxy-target-exclusion contracts, not a new scope decision. Estimated compute is below the material-cost gate.

Unit 2F statistical hardening on 2026-07-13: individual subgroup eligibility is fixed once on the complete OOF sample before resampling. A paired policy gap uses only the intersection of groups eligible in both policies for that exact attribute/metric/class estimand; fewer than two common groups yields an explicit insufficient-support status and no point/interval estimate. The configured batch size of 200 is now executed for subgroup and proxy bootstrap metric calculations. All subgroup/proxy intervals and observed rankings are pointwise descriptive with no multiplicity-adjusted, simultaneous, discrimination, fairness or causal claim. The proxy `(outer_fold, EmpDepartment)` strata use a hashed, explicit adapter to the central bootstrap utility's internal `y_true` field; PerformanceRating is never used for that proxy resampling. These corrections resolve implementation defects without changing an approved scientific choice.

Unit 2F final review consequence on 2026-07-13: department reconstructability is a distinct `nominal_multiclass_proxy_diagnostic`, never a restricted employee-performance task. Its identity-bound label mapping and manuscript-facing rows must report overall/fold class support, zero-support cells and the fixed-model conditional-inference scope. The watchlist is config-owned, boolean seeds fail closed, audit attributes join by explicit sample identity and persisted policy feature counts are revalidated. This tightens task/support/provenance boundaries without changing an approved scientific scope or requiring a user decision.

Accepted Unit 2G policy A on 2026-07-13: the HRDataset primary replication is conservative. It excludes Salary, State, Zip, RecruitmentSource, DeptID, PositionID, MarriedID, EmpStatusID, FromDiversityJobFairID and raw DateOfHire in addition to all declared ID/target/leakage/sensitive fields; department text is excluded and job-role text may remain. Scientifically useful proxy-rich alternatives are audit/sensitivity models only and can never be the primary external result. EngagementSurvey, EmpJobSatisfaction, SpecialProjectsCount, DaysLateLast30 and Absences may remain only with explicit cross-sectional temporality limitations and a separate sensitivity model removing them. The target mapping remains unchanged. If these removals make the task technically infeasible or change mapping interpretation rather than performance, implementation must stop and return to the user.

Still prohibited without explicit approval: force-push, merge, release publication, Git history alteration, unapproved mirror use, or new scientific-protocol choices.

V2-015 provenance decision on 2026-07-14: the canonical experiment input remains the pinned semicolon CSV; the tracked BIFF8 workbook is a provenance source only. Workbook/CSV equivalence is exact only after one predeclared trim/blank/numeric/date normalization contract and must be published as an atomic compact clean-Git receipt containing hashes, shapes and mismatch coordinates but no employee values. The workbook codebook's `RelationshipSatisfaction` block is explicitly mapped to data column `EmpRelationshipSatisfaction`; no implicit fuzzy matching is allowed. Because the sheet documents seven of 28 columns, it is partial codebook evidence and cannot establish a complete data dictionary, semantic authority, source authenticity, licence or citation.

V2-014 publication-hygiene decision on 2026-07-14: D3 is implemented forward-only. The five user-provided source inputs plus one interim and eight row-level/fitted working-data artifacts are removed from the current Git index with local bytes preserved and ignored. Cards/mappings/placeholders remain tracked. A separate technical export is built from an exact commit with an explicit allowlist using `git archive`, so it carries no `.git` history or forbidden local-data member. This does not erase prior commits, establish redistribution permission, publish a release, or admit historical outputs. History rewriting and force-push remain prohibited.

Unit 2G implementation consequence on 2026-07-13: conservative policy A remains technically feasible with seven exact primary features and unchanged mapped support 31/243/37. `ExperienceYearsAtThisCompany` is derived only from source dates before modelling; two negative source durations are deterministically converted to missing and recorded, while raw dates remain forbidden and imputation remains training-partition-only. This is a data-quality enforcement choice, not a target or scientific-scope change.

Unit 2G claim boundary on 2026-07-13: the department proxy task contains a singleton department class, so at least one outer-training partition cannot contain all nominal classes. The canonical diagnostic must therefore publish `not_estimated_insufficient_outer_training_class_support`; classes will not be merged or dropped without a new user decision. The absence of an estimate is evidence of insufficient support, not evidence of low or high proxy risk.

Unit 2G identity hardening on 2026-07-13: an external stage may not trust caller-supplied digest-shaped strings. It independently recomputes the core scope hash and composite scientific-input hash from the loaded config, exact core dataset receipt set and exact scoped side-input set, including feature taxonomy. It also checks actual HEAD/source/worktree both before computation and before atomic publication. This implements the already accepted provenance/fail-closed rules and changes no model protocol.

Post-Unit-2G recovery decision on 2026-07-14: independent closed-world, model-replay, calibration-replay, OOF, SHAP, support and claim-boundary audits found no P0/P1 defect in the completed atomic external stage. The outer input manifest is deliberately provisional and the stage is not a canonical package. Therefore Unit 2G will not be rerun merely to manufacture a package-level completion receipt; its evidence remains local stage-validation input to the later clean all-stage rebuild.

Reporting boundary on 2026-07-14: predeclared sigmoid calibration may be described as improving probability quality for this stage because paired log-loss and Brier improvements are positive, but it may not be described as improving predictive performance generally. It lowers macro-F1 and yields zero class-4 argmax predictions. The temporality-restricted result must be reported as a major limitation, and SHAP remains noncausal raw-margin attribution. No stage-validation number is frozen for the manuscript.

D5 remediation decision on 2026-07-14: commit `e25f403` had already pushed the 65.4 MB full noncanonical stage package contrary to the accepted small-artifacts-in-Git policy. Rewriting history is prohibited. The conservative forward remedy is to preserve all 126 validated files locally, remove them only from the current Git tip with `git rm --cached`, add an exact ignore rule, and retain the accidental historical blobs as an explicit repository/publication blocker. This changes neither scientific output nor its hashes.

V2-029 reporting-contract consequence on 2026-07-14: external subgroup diagnostics must identify config-owned `probability_method=raw` and publish a deterministic semantic SHA-256 over the exact policy-scoped OOF rows consumed, with scope/algorithm/columns explicitly distinguished from the byte hash of the full `raw_oof_predictions.csv` artifact. Grouped SHAP values are XGBoost raw-margin-score contributions; provider, canonical config, tables, fold receipts, metadata and local reason-code renderings must agree on that unit and fail closed on drift. This adds explicit semantics without changing any model, fold, target, probability, attribution value or prior stage artifact.

V2-012 supplementary-search consequence on 2026-07-14: counterfactual evidence remains supplementary and is described only as heuristic counterfactual-search success. Four distinct taxonomy-labelled feature scopes are reported independently. Restricted, primary and expanded budgets filter one shared maximum pool within each case/scope; this supports within-scope budget inclusion but no cross-scope monotonicity claim. Search-success rates use Wilson intervals; probability-gain/cost/sparsity summaries use 5,000-draw percentile bootstrap conditional on successful cases. Taxonomy control labels do not establish practical controllability, causality, employee actionability, intervention validity, advice or real-world feasibility. This freezes implementation/reporting consequences of the already accepted supplementary-only boundary and introduces no new core scientific claim.

V2-013 supplementary-external consequence on 2026-07-14: IBM PerformanceRating is retained only as a restricted binary 3/4 robustness stratum with class 4 predeclared positive; IBM attrition and Employee Turnover remain separate related binary task-transfer strata. Macro-F1 is selected within each task's primary conservative policy; balanced accuracy, not QWK or another ordinal metric, is the task-valid deterministic tie-break. The fold-selected candidate is reused across same-fold audit policies. Raw binary ranking/probability metrics apply; ordinal distance/QWK/severe-error and multiclass Brier values are literal `N/A`. The three strata are not directly comparable with the primary task, direct external validation or transport. This is a task-applicability consequence of the fixed conservative strategy, not a new core claim.

V2-014 completion consequence on 2026-07-14: clean commit `9342b0c` and the independently rebuilt `12_publication_export_receipt.json` satisfy the accepted forward-only sanitation contract. The compact receipt may be called canonical-eligible only for that narrow technical-export contract; it is not a canonical scientific package or a published release. The validation ZIP remains ephemeral. Prior public Git objects, source/licence/citation authority, redistribution permission and release publication remain separate manual blockers.

V2-018 dependency consequence on 2026-07-14: the compatibility entry point installs only the canonical scientific core. Approved supplementary stages introduce no additional third-party package. Retained CatBoost/UI/OpenAI modules are isolated in `requirements-legacy-optional.txt` and enter only the full development environment because historical guardrail tests import them; they remain excluded from core and supplementary evidence. CPython 3.14 plus one exact constraints file is the controlled release baseline. Package installation may contact the configured index, but scientific execution must later pass the separate V2-020 no-network gate.

V2-018 completion consequence on 2026-07-14: `13_dependency_lock_receipt.json` is canonical-eligible only for the exact clean core dependency environment at commit `498e8ad`. It proves the tracked dependency taxonomy/lock and one isolated Windows CPython 3.14 installation; it does not replace cross-platform CI, assert future index availability, authorize scientific network access, or admit a scientific result.

V2-020 runtime consequence on 2026-07-14: package installation/index access remains outside scientific execution. Every public core or supplementary build is now process-locally offline from its first implementation call through final package validation. Complete package status is impossible after any attempted DNS/socket/shell/nonlocal subprocess operation, even if stage code catches the immediate exception. Only exact read-only local Git provenance subcommands are allowed. This is an execution-safety contract, not evidence that future hosted infrastructure or external package sources are available.

V2-022 integration consequence on 2026-07-14: the task schema's applicability list is the complete authoritative metric registry for that task, not a stage display order. Policy ablation and calibration retain explicit predeclared report subsets, require those metrics to remain applicable, reject invalid or duplicate applicability entries, and ignore valid registry metrics outside their report scope. This repairs execution against the already accepted complete registry without changing any metric definition, selection rule, estimand, fold, model or scientific result.

V2-017 migration consequence on 2026-07-14: the former physical `latest` is a redundant alias, not the sole historical package. Every common file is byte-identical to the separately tracked immutable named v1 run; the sole extra file is a pointer naming that run. The safe forward migration removes only the duplicate tracked alias after recording exact inventory/preservation evidence, reserves `latest` for one atomic v2 pointer, and does not rewrite history or delete the named evidence. Strict current-HEAD validation means the already valid pre-migration candidate cannot be relabelled; a new exact-migration-commit build is required.

V2-033 atomic-publication consequence on 2026-07-14: transient Windows antivirus/indexer-style sharing violations may be retried only at the final absent-destination directory rename, for a fixed bounded duration and only for WinError 5/32/33. Existing destinations, missing staging, other errors and exhausted retries still fail closed. Cleanup failure may never mask the primary publication error; it is attached as an exception note. This changes no scientific computation or artifact content.

## Compact manuscript-support publication decision - 2026-07-16

- Keep the complete 545-file canonical evidence root local and ignored. The earlier 50-file aggregate admission recorded by receipt 16 is superseded at the current tip, but the receipt remains historical chronology.
- Track only the deterministic compact package under `manuscript/mdpi_information/assets/`. It carries manuscript-ready figures/tables plus complete source maps, full-precision numerical records, claims, captions, alt text, and a closed-world manifest; it carries no row-level evidence, raw data, fitted model, bootstrap index, or secret.
- Reuse `canonical_v2_20260714T221501Z_483f96f` without any scientific rerun. The export utility is outside the frozen scientific source tree and may only verify, copy, or presentation-render already frozen contracts/data.
- Map manuscript Figures 1-7 to canonical Figures 1, 3, 2, 4, 5, 6, 7. Figure 1 is a clearly labelled post-canonical presentation rendering because the canonical version did not fully express both conceptual rationale and training/test flow; it adds no scientific value.
- Do not edit the journal manuscript in this task. Manual insertion, claim-matrix approval, ethics/licence decisions, and release/DOI publication remain external.

## Major-revision v3 information-contract decision — 2026-09-03

- Treat the prediction point as a conceptual pre-evaluation-completion estimand only. The cross-sectional INX source contains no observed feature or decision timestamps, so prospective availability is not empirically verified.
- Use eight mutually explicit risk labels: identifier, target-direct leakage, temporal leakage, outcome-proximal, timing-uncertain, sensitive attribute, organisational proxy, and ordinary predictor. Sensitive-feature removal is governance control, not leakage removal; proxy removal is a separate sensitivity.
- Freeze six cumulative information policies. P0 removes only identifier/target; P1 additionally removes temporal/outcome-proximal risks; P2 additionally removes direct sensitive fields; P3 additionally removes Department for v2 primary continuity; P4 additionally removes all declared timing-uncertain fields; P5 additionally removes declared organisational proxies.
- Call P4 `STRICT_PROSPECTIVE` only as a prospective-plausibility sensitivity and P5 `STRICT_PROXY` only as a declared-proxy sensitivity. Neither proves leakage elimination, absence of residual proxies, fairness, or deployment readiness.
- Preserve the complete v2 config/artifacts. The v3 contract is an additive scientific side input; new experiment output must receive a new `canonical_v3_*` identity.

## Major-revision v3 ordinal-benchmark decision — 2026-09-03

- Reuse the four exact canonical-v2 nominal OOF prediction sets without refitting or relabelling them. Fit the new ordinal models and naive baselines on the exact same hash-bound 10×5 fold assignments and the same P3 feature set; this preserves an apples-to-apples comparison while keeping v2 immutable.
- Add two genuinely ordinal models: a proportional-odds cumulative-logit estimator with ordered thresholds, and a nonlinear cumulative-threshold XGBoost estimator with rowwise non-increasing PAVA correction before class-probability differencing.
- Add majority, seeded stratified-random, and lower ordinal-median baselines. They learn class statistics from each outer-training partition only and never enter hyperparameter selection.
- Use macro-F1 as the inner selection metric and QWK as the deterministic tie-break inside the frozen 0.001 macro-F1 tolerance. Outer-test data remain evaluation-only.
- Report normalized RPS and rename the manuscript-facing extreme error to `two_level_reversal_rate`, defined by an absolute declared-class-position error of at least two. Preserve the legacy v2 field only as historical evidence, not as the v3 display name.
- Keep XGBoost as the prespecified XAI reference because of exact-fold TreeSHAP continuity, not because of predictive superiority. Report metric-specific rankings and prohibit a universal-best-model claim.
- Require a clean Git worktree, exact commit, complete source-tree hash, bound dataset/config/fold/OOF hashes, and unchanged pre/post execution identities before atomically publishing any full result. Local row-level OOF outputs remain ignored and unpublishable.

## Major-revision v3 ordinal-result publication decision — 2026-09-03

- Admit `phase1b_v3_20260903T130912Z_dc5cb8b` as complete Phase 1B evidence after independent closed-world rehashing, exact generation-blob/source binding, OOF-lineage checks, and numerical recomputation. It is a phase result, not an integrated `canonical_v3_*` release.
- Publish only the nine-file compact package under `reports/research_log/major_revision_v3/phase1b_ordinal_benchmark/`. Keep employee-level OOF rows and the complete local run Git-ignored; do not publish fitted models or raw data.
- Report rankings per metric. Cumulative-threshold XGBoost may be described as leading macro-F1/balanced accuracy, Random Forest as leading QWK/ordinal MAE, LightGBM as leading RPS/Brier, and nominal XGBoost as leading log loss. A universal-best-model claim is prohibited.
- Preserve adverse/null evidence: proportional-odds logistic is weaker than the nominal comparators, cumulative-threshold XGBoost has poor raw log loss despite its classification ranking, and naive middle-class prediction can obtain zero two-level reversals without useful class discrimination.
- Do not attach significance or training-variability language to the 0.0044 macro-F1 contrast between cumulative-threshold and nominal XGBoost until the separately prespecified repeated-CV sensitivity is complete.

## Major-revision v3 repeated nested-CV decision — 2026-09-04

- Select the reviewer-authorized bounded design of five repetitions × five outer folds × five inner folds before inspecting repeated-CV results. The all-nine-system design requires 5,725 estimator-fit calls; ten repetitions would require 11,450. A future escalation cannot be chosen from these results and requires a new prespecified contract.
- Freeze exact outer/inner/model seed triples as 1042/1043/1044 through 5042/5043/5044. All models share folds within a repetition, all semantic outer assignments must differ across repetitions, and no seed or repetition may be discarded or selected by performance.
- Refit all four nominal, two ordinal, and three naive systems in every repetition. Canonical-v2 OOF reuse is prohibited for this training/fold-variability estimand. The six tuned families retain their exact candidate registries and training-only macro-F1/QWK selection rule.
- Report mean, sample SD, median, minimum, and maximum for macro-F1, balanced accuracy, QWK, and ordinal MAE. Treat the range as descriptive repetition variation, not a confidence interval. Report six-model rank/winner/pairwise-Spearman stability and prohibit a universal-winner claim.
- Keep fold assignments, employee-level OOF rows, and fitted models local and ignored. Only a separately validated compact aggregate/rank/provenance export may enter Git.

## Major-revision v3 repeated nested-CV result decision — 2026-09-04

- Admit `phase1c_v3_20260903T215015Z_78649c4` as complete Phase 1C evidence after independent byte, generation-source, fold-reconstruction, selection-lineage, OOF, metric, variability, rank, stability, and tuning-frequency validation. It remains a phase result rather than an integrated canonical-v3 release.
- Publish only the nine-file compact package under `reports/research_log/major_revision_v3/phase1c_repeated_nested_cv/`. Keep the 12-file complete local run, 54,000 employee-level OOF rows, fold contracts, fold metrics, candidate-search rows, and selected fold records ignored.
- Report the classification ordering as variable: LightGBM wins macro-F1 in 3/5 repetitions and XGBoost in 2/5; cumulative-threshold XGBoost wins balanced accuracy in 4/5 and LightGBM in 1/5. Do not convert mean or winner frequency into a universal-best claim.
- Report Random Forest's 5/5 QWK and ordinal-MAE wins as stability within these five prespecified repetitions only. Do not generalize this to prospective, causal, fairness, deployment, or all-metric superiority.
- Treat sample SD and minimum–maximum values as descriptive repetition variability, never as sample-level uncertainty or confidence intervals. Retain the weaker proportional-odds result and every adverse or unstable result.

## Major-revision v3 policy-retuning decision — 2026-09-04

- Freeze two separate estimands. The fixed primary-schedule estimand changes feature access while reusing the P3 fold-specific XGBoost candidate schedule; the independently retuned estimand repeats training-only candidate selection within every policy and outer fold. Neither difference is causal.
- Use the exact Phase 1A P0–P5 feature sets and exact canonical-v2 10×5 fold assignments. Reuse fixed OOF rows for P0–P3 only after exact feature-set verification; newly fit P4/P5 under the primary schedule. Preserve nonmatching historical v2 policies unchanged but exclude them from the six-policy v3 table.
- Restrict the model to the prespecified eight-candidate XGBoost registry, use inner macro-F1 with QWK tie-breaking inside 0.001, and prohibit outer-test participation in preprocessing, tuning, candidate selection, or policy choice. Retuned P3 must exactly replay the canonical primary schedule, labels, and probabilities.
- Report all 16 metrics for both estimands and headline macro-F1, QWK, balanced accuracy, and ordinal MAE. Publish both raw `retuned - fixed` differences and direction-aligned improvements without significance or universal-best-policy language.
- Keep OOF, folds, candidate-search rows, and fitted models local and ignored. A later compact export may contain aggregate metrics, comparisons, candidate frequencies, and provenance only after independent validation.

## Major-revision v3 policy-retuning result decision — 2026-09-04

- Admit `phase1d_v3_20260904T063324Z_823c848` as complete Phase 1D evidence after independent closed-world byte validation, generation-source binding, exact P0–P3 reuse proof, all-60 selection replay, exactly-once OOF checks, zero-error P3 replay, and full fold/aggregate/comparison recomputation. It remains a phase result rather than an integrated canonical-v3 release.
- Publish only the seven-file compact package under `reports/research_log/major_revision_v3/phase1d_policy_retuning/`. Keep 14,400 employee-level OOF rows, folds, fold metrics, 480 candidate rows, 60 selected fold records, raw data, and fitted models local and ignored.
- Report that retuning raises macro-F1 point estimates for P0/P1/P2/P4/P5 and leaves P3 unchanged, but retain mixed/adverse criteria: P0 balanced accuracy declines; P5 QWK and balanced accuracy decline despite macro-F1 and ordinal-MAE gains; P4 ordinal MAE is unchanged.
- Treat P0 only as an information-rich diagnostic upper bound because it retains outcome-proximal/timing-risk fields. Do not interpret P4 as prospective validation or P5 as absence of residual proxies/fairness.
- Treat every fixed-versus-retuned contrast as descriptive and conditional on the exact candidate grid/folds. No significance, causal effect, universal-best-policy, leakage-free, or deployment-ready claim is authorized.

## Major-revision v3 SHAP stability/faithfulness decision — 2026-09-04

- Preserve the exact canonical-v2 outer-fold models and fold-pair stability as historical reference. Formalize the actual TreeSHAP computation as no-background tree-path-dependent raw-margin explanation, normalize sample/class/transformed axes, sum signed encoded values within raw families, then take absolute values and average across classes/samples.
- Require per-sample/per-class grouped-sum preservation and expected-value-plus-SHAP raw-margin additivity within `1e-5`. Probability-space SHAP and absolute-before-grouping are prohibited for this experiment.
- Add five new fixed-fold model-seed runs while holding all training rows, candidates, preprocessing, and folds fixed; include the exact seed-42 canonical model set as the sixth comparison. Separately add five stratified 80% subsamples within each outer-training partition with fixed candidate schedule and model seed. Report top-5/10/15 Jaccard and all-feature Spearman without pair-independence or confidence-interval claims.
- Define faithfulness only on exact prediction-producing canonical fold models. For every OOF sample, mask top-1/3/5 predicted-class SHAP families using outer-training numeric medians/categorical modes and compare probability/margin drops plus deletion AUC with 20 nested random feature-set repetitions.
- State that median/mode masking can create out-of-distribution hybrids. The result is model-level deletion faithfulness only, never causal importance, realistic intervention, human usefulness, fairness, recommendation, or deployment evidence.
- Keep local SHAP/deletion rows, resample memberships, and fitted models ignored. Only independently validated aggregate receipts, stability summaries, faithfulness contrasts, deletion-AUC summaries, and provenance may enter Git.
