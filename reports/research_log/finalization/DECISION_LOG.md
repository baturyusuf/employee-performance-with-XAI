# Persistent Decision Log

The fixed decisions are recorded in `../finalization_v2/00_scope_and_fixed_decisions.md` and `../finalization_v2/04_decision_log.md`.

Accepted on 2026-07-13:

- D1: Logistic Regression + Random Forest + LightGBM.
- D2: restrained nested tuning with 10 outer × 5 inner folds and outer-test isolation.
- D3: user-provided pinned files for all datasets; sanitized publication export; only approved-manifest URL acquisition when local data is absent.
- D4: application pending; institution/unit/reference/date remain unknown because supplied strings were placeholders.
- D5: small artifacts in Git; full package prepared for Release/Zenodo; pointer-only latest; no publication yet.
- Dedicated branch/commit permission: `finalization/leakage-aware-v2`, tested checkpoints only.
- Model selection: macro-F1 is the sole primary inner-CV metric; QWK is secondary and is used only inside the inclusive absolute `0.001` macro-F1 tie pool, followed by lowest candidate index.
- Stop gate: trigger only when baseline-minus-XGBoost macro-F1 is positive and the paired OOF bootstrap 95% CI lower bound is greater than zero; then request user direction before choosing the XAI reference model.
- Bootstrap: 5,000 paired sample-level OOF draws, stratified by outer fold and true class, percentile 95% interval with linear quantiles.

Observed on 2026-07-13: verified trial `benchmark-10x5-20260713-6a80074` did not trigger the baseline superiority gate. XGBoost therefore remains the predeclared XAI reference without a new user decision. Secondary QWK differences remain reportable but do not override the accepted macro-F1 gate.

Engineering decision on 2026-07-13: canonical SHAP must load the exact prediction-producing XGBoost pipeline for each outer fold, replay its OOF evidence at `1e-12` probability tolerance, and contain no model/preprocessor fit or splitter path. The 45 fold pairs are dependent descriptive units, so no pairwise t confidence interval is reported. The historical `6a80074` trial remains immutable and reader-verifiable at its documented `1e-6` tolerance, but its pre-warning-cleanup config/probabilities and missing nested one-hot feature-name lineage make it intentionally ineligible as a canonical SHAP upstream. No user scientific decision was required.

Engineering/scientific consequence of accepted D2 on 2026-07-13: leakage-policy variants are a matched feature-access sensitivity analysis, not independently optimized leaderboard entries. The exact benchmark OOF rows are reused for the primary policy; every other declared policy receives the primary policy's selected XGBoost candidate from the same outer fold. All policy intervals/differences use the same 5,000 paired sample-level resamples as the benchmark. The legacy full-feature name is retained, but its role is an information-rich diagnostic comparator whose superiority is not guaranteed. No new user decision was required because D2 already says not to tune each leakage policy independently.

Accepted on 2026-07-13: calibration option A. For each outer fold, five inner-fold XGBoost refits must generate cross-fitted probabilities for every outer-training sample exactly once. The sigmoid calibrator may see only those inner-OOF probabilities and labels. It is applied only to the exact persisted benchmark fold model's untouched outer-test probabilities. Outer-test data cannot influence tuning, calibrator fitting, method selection or threshold selection. Raw is a comparator, sigmoid is the fixed primary method, and isotonic is excluded from the core protocol.

Unit 2E engineering decisions on 2026-07-13: use the warning-free scikit-learn `>=1.8,<1.9` L2 API with one numerical thread; preserve upstream float probabilities exactly and normalize only the calibrated projection; freeze 95% paired percentile bootstrap strata to outer fold plus true class; and revalidate every config/data/fold/benchmark/model input immediately before atomic publication. The current config hash must reject the historical benchmark hash. The expensive rerun is deferred until all remaining core config and hashed inputs freeze, avoiding another knowingly stale result package. These choices do not alter the accepted model, folds, primary metric or sigmoid decision.

Unit 2F engineering consequence on 2026-07-13: subgroup diagnostics will consume the exact raw OOF probabilities and labels from the three declared `policy_ablation` systems; they will not fit a performance model or mix calibrated primary probabilities with raw comparison-policy probabilities. Their 5,000-draw plan must reproduce the accepted `(outer_fold, y_true)` policy resample hash. Department reconstructability will use the exact shared outer-fold assignment but a separate 5,000-draw plan stratified by `(outer_fold, EmpDepartment)` because it is a different target. The proxy target is removed before feature-contract hashing; therefore the department-including policy is an explicit alias of the department-free predictor contract and is not refitted. These are required consequences of the accepted shared-fold, support-aware and proxy-target-exclusion contracts, not a new scope decision. Estimated compute is below the material-cost gate.

Unit 2F statistical hardening on 2026-07-13: individual subgroup eligibility is fixed once on the complete OOF sample before resampling. A paired policy gap uses only the intersection of groups eligible in both policies for that exact attribute/metric/class estimand; fewer than two common groups yields an explicit insufficient-support status and no point/interval estimate. The configured batch size of 200 is now executed for subgroup and proxy bootstrap metric calculations. All subgroup/proxy intervals and observed rankings are pointwise descriptive with no multiplicity-adjusted, simultaneous, discrimination, fairness or causal claim. The proxy `(outer_fold, EmpDepartment)` strata use a hashed, explicit adapter to the central bootstrap utility's internal `y_true` field; PerformanceRating is never used for that proxy resampling. These corrections resolve implementation defects without changing an approved scientific choice.

Unit 2F final review consequence on 2026-07-13: department reconstructability is a distinct `nominal_multiclass_proxy_diagnostic`, never a restricted employee-performance task. Its identity-bound label mapping and manuscript-facing rows must report overall/fold class support, zero-support cells and the fixed-model conditional-inference scope. The watchlist is config-owned, boolean seeds fail closed, audit attributes join by explicit sample identity and persisted policy feature counts are revalidated. This tightens task/support/provenance boundaries without changing an approved scientific scope or requiring a user decision.

Accepted Unit 2G policy A on 2026-07-13: the HRDataset primary replication is conservative. It excludes Salary, State, Zip, RecruitmentSource, DeptID, PositionID, MarriedID, EmpStatusID, FromDiversityJobFairID and raw DateOfHire in addition to all declared ID/target/leakage/sensitive fields; department text is excluded and job-role text may remain. Scientifically useful proxy-rich alternatives are audit/sensitivity models only and can never be the primary external result. EngagementSurvey, EmpJobSatisfaction, SpecialProjectsCount, DaysLateLast30 and Absences may remain only with explicit cross-sectional temporality limitations and a separate sensitivity model removing them. The target mapping remains unchanged. If these removals make the task technically infeasible or change mapping interpretation rather than performance, implementation must stop and return to the user.

Still prohibited without explicit approval: force-push, merge, release publication, Git history alteration, unapproved mirror use, or new scientific-protocol choices.

V2-015 provenance decision on 2026-07-14: the canonical experiment input remains the pinned semicolon CSV; the tracked BIFF8 workbook is a provenance source only. Workbook/CSV equivalence is exact only after one predeclared trim/blank/numeric/date normalization contract and must be published as an atomic compact clean-Git receipt containing hashes, shapes and mismatch coordinates but no employee values. The workbook codebook's `RelationshipSatisfaction` block is explicitly mapped to data column `EmpRelationshipSatisfaction`; no implicit fuzzy matching is allowed. Because the sheet documents seven of 28 columns, it is partial codebook evidence and cannot establish a complete data dictionary, semantic authority, source authenticity, licence or citation.

V2-014 publication-hygiene decision on 2026-07-14: D3 is implemented forward-only. The five user-provided source inputs plus one interim and eight row-level/fitted working-data artifacts are removed from the current Git index with local bytes preserved and ignored. Cards/mappings/placeholders remain tracked. A separate technical export is built from an exact commit with an explicit allowlist using `git archive`, so it carries no `.git` history or forbidden local-data member. This does not erase prior commits, establish redistribution permission, publish a release, or admit historical outputs. History rewriting and force-push remain prohibited.

Unit 2G implementation consequence on 2026-07-13: conservative policy A remains technically feasible with seven exact primary features and unchanged mapped support 31/243/37. `ExperienceYearsAtThisCompany` is derived only from source dates before modelling; two negative source durations are deterministically converted to missing and recorded, while raw dates remain forbidden and imputation remains training-partition-only. This is a data-quality enforcement choice, not a target or scientific-scope change.

Unit 2G claim boundary on 2026-07-13: the department proxy task contains a singleton department class, so at least one outer-training partition cannot contain all nominal classes. The canonical diagnostic must therefore publish `not_estimated_insufficient_outer_training_class_support`; classes will not be merged or dropped without a new user decision. The absence of an estimate is evidence of insufficient support, not evidence of low or high proxy risk.

Unit 2G identity hardening on 2026-07-13: an external stage may not trust caller-supplied digest-shaped strings. It independently recomputes the core scope hash and composite scientific-input hash from the loaded config, exact core dataset receipt set and exact scoped side-input set, including feature taxonomy. It also checks actual HEAD/source/worktree both before computation and before atomic publication. This implements the already accepted provenance/fail-closed rules and changes no model protocol.

Post-Unit-2G recovery decision on 2026-07-14: independent closed-world, model-replay, calibration-replay, OOF, SHAP, support and claim-boundary audits found no P0/P1 defect in the completed atomic external stage. The outer input manifest is deliberately provisional and the stage is not a canonical package. Therefore Unit 2G will not be rerun merely to manufacture a package-level completion receipt; its evidence remains local stage-validation input to the later clean all-stage rebuild.

Reporting boundary on 2026-07-14: predeclared sigmoid calibration may be described as improving probability quality for this stage because paired log-loss and Brier improvements are positive, but it may not be described as improving predictive performance generally. It lowers macro-F1 and yields zero class-4 argmax predictions. The temporality-restricted result must be reported as a major limitation, and SHAP remains noncausal raw-margin attribution. No stage-validation number is frozen for the manuscript.

D5 remediation decision on 2026-07-14: commit `e25f403` had already pushed the 65.4 MB full noncanonical stage package contrary to the accepted small-artifacts-in-Git policy. Rewriting history is prohibited. The conservative forward remedy is to preserve all 126 validated files locally, remove them only from the current Git tip with `git rm --cached`, add an exact ignore rule, and retain the accidental historical blobs as an explicit repository/publication blocker. This changes neither scientific output nor its hashes.

V2-029 reporting-contract consequence on 2026-07-14: external subgroup diagnostics must identify config-owned `probability_method=raw` and publish a deterministic semantic SHA-256 over the exact policy-scoped OOF rows consumed, with scope/algorithm/columns explicitly distinguished from the byte hash of the full `raw_oof_predictions.csv` artifact. Grouped SHAP values are XGBoost raw-margin-score contributions; provider, canonical config, tables, fold receipts, metadata and local reason-code renderings must agree on that unit and fail closed on drift. This adds explicit semantics without changing any model, fold, target, probability, attribution value or prior stage artifact.

V2-012 supplementary-search consequence on 2026-07-14: counterfactual evidence remains supplementary and is described only as heuristic counterfactual-search success. Four distinct taxonomy-labelled feature scopes are reported independently. Restricted, primary and expanded budgets filter one shared maximum pool within each case/scope; this supports within-scope budget inclusion but no cross-scope monotonicity claim. Search-success rates use Wilson intervals; probability-gain/cost/sparsity summaries use 5,000-draw percentile bootstrap conditional on successful cases. Taxonomy control labels do not establish practical controllability, causality, employee actionability, intervention validity, advice or real-world feasibility. This freezes implementation/reporting consequences of the already accepted supplementary-only boundary and introduces no new core scientific claim.

V2-013 supplementary-external consequence on 2026-07-14: IBM PerformanceRating is retained only as a restricted binary 3/4 robustness stratum with class 4 predeclared positive; IBM attrition and Employee Turnover remain separate related binary task-transfer strata. Macro-F1 is selected within each task's primary conservative policy; balanced accuracy, not QWK or another ordinal metric, is the task-valid deterministic tie-break. The fold-selected candidate is reused across same-fold audit policies. Raw binary ranking/probability metrics apply; ordinal distance/QWK/severe-error and multiclass Brier values are literal `N/A`. The three strata are not directly comparable with the primary task, direct external validation or transport. This is a task-applicability consequence of the fixed conservative strategy, not a new core claim.

V2-014 completion consequence on 2026-07-14: clean commit `9342b0c` and the independently rebuilt `12_publication_export_receipt.json` satisfy the accepted forward-only sanitation contract. The compact receipt may be called canonical-eligible only for that narrow technical-export contract; it is not a canonical scientific package or a published release. The validation ZIP remains ephemeral. Prior public Git objects, source/licence/citation authority, redistribution permission and release publication remain separate manual blockers.
