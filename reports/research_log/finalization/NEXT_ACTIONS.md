# Next Actions

1. Review the complete Unit 1B diff and create the authorized tested checkpoint on `finalization/leakage-aware-v2`.
2. Record the next unit's problem, root cause, intended files, acceptance criteria and tests before production edits.
3. Isolate a core-only stage graph with no LLM/chatbot/API path and move counterfactual/related-task evidence to supplementary orchestration.
4. Implement one immutable shared outer-fold assignment and versioned restrained nested-tuning contract for XGBoost plus the three approved baselines.
5. Run the baseline-over-XGBoost paired OOF bootstrap gate before finalizing the XAI reference model.

Do not run or reuse the old `reports/manuscript_final/latest` package as v2 evidence. Do not edit the manuscript. Do not make API calls.
