# Next Actions

1. Create the authorized Unit 1A checkpoint commit on `finalization/leakage-aware-v2`.
2. Record Unit 1B problem/root cause/intended files/acceptance tests before edits.
3. Add failing tests for side-input hashes, actual-receipt/manifest identity and cache invalidation.
4. Bind acquisition receipts, schema mappings, feature taxonomy, provenance, search spaces and task/metric inputs into run manifests and snapshots.
5. Run focused tests and a real-data manifest preflight; checkpoint only if the unit is coherent and tests pass.

Do not run or reuse the old `reports/manuscript_final/latest` package as v2 evidence. Do not edit the manuscript. Do not make API calls.
