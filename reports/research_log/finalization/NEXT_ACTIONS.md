# Next Actions

1. Review and checkpoint the fully tested Unit 2A scope-isolation implementation.
2. Record Unit 2B problem/root cause/intended files/acceptance tests before scientific code edits.
3. Implement one immutable shared outer-fold assignment and restrained nested tuning for XGBoost plus Logistic Regression, Random Forest and LightGBM.
4. Generate sample-level OOF predictions and paired 5,000-resample stratified bootstrap model comparisons under the primary policy.
5. Apply the baseline-over-XGBoost stop gate before finalizing the XAI reference model or proceeding to its regenerated XAI evidence.

Do not run or reuse the old `reports/manuscript_final/latest` package as v2 evidence. Do not edit the manuscript. Do not make API calls.
